<!DOCTYPE html>
<html>

  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>餐廳評論網</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css"
          integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
  </head>

  <body>
    {{!-- Navbar --}}
    <nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-dark">
      <a class="navbar-brand" href="/">餐廳評論網</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
              aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
        <ul class="navbar-nav align-items-center">
          {{#if user}}
            <li class="nav-item">
              <span style="color: white;margin-right: 10px;">Hi, {{user.email}}</span>
            </li>
          {{/if}}
          {{#if user}}
            <div class="d-flex justify-content-around align-items-center mt-sm-3 mt-md-3 mt-lg-0">
              <li class="nav-item">
                <a class="btn btn-outline-secondary text-white border-white my-sm-0 ml-3" href="/">Home</a>
              </li>
              <li class="nav-item">
                <a class="btn btn-outline-info my-sm-0 ml-3" href="/users/{{user.id}}">用戶資訊</a>
              </li>
              <li class="nav-item">
                <a class="ml-3" href="/logout"><button class="btn btn-outline-danger my-2 my-sm-0">LogOut</button></a>
              </li>
          {{/if}}
              {{#if user.isAdmin}}
                <div class="dropdown">
                  <button class="btn btn-outline-success my-sm-0 dropdown-toggle ml-3" type="button" id="adminDropdown" data-toggle="dropdown"
                          aria-haspopup="true" aria-expanded="false">
                    Admin 後台
                  </button>
                  <div class="dropdown-menu" aria-labelledby="adminDropdown">
                    <a class="dropdown-item" href="/admin">後台主頁</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="/admin/users">用戶管理</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="/admin/categories">餐廳類別</a>
                    <a class="dropdown-item" href="/admin/restaurants">餐廳列表</a>
                  </div>
                </div>
              {{/if}}
          </div>
        </ul>
      </div>
    </nav>
    <br />
    <main role="main">
      <div class="album py-5 bg-light">
        <div class="container">
          {{#if success_messages}}
            <div class="alert alert-success" role="alert" style="margin: 30px 0 ;">
              {{success_messages}}
            </div>
          {{/if}}
          {{#if error_messages}}
            <div class="alert alert-danger" role="alert" style="margin: 30px 0 ;">
              {{error_messages}}
            </div>
          {{/if}}

          {{{body}}}
        </div>
      </div>
    </main>
  </body>
  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"
          integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo" crossorigin="anonymous"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"
          integrity="sha384-ZMP7rVo3mIykV+2+9J3UJ46jBk0WLaUAdn689aCwoqbBJiSnjAK/l8WvCWPIPm49" crossorigin="anonymous"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"
          integrity="sha384-ChfqqxuZUCnJSK3+MXmPNIyE6ZbWh2IMqE241rYiqJxyMiZ6OW/JmZQ5stwEULTy" crossorigin="anonymous"></script>

</html>